# SentinelPay — Build Report

Generated by: developer agent  
Architecture source: ARCHITECTURE.md + API_CONTRACTS.md

---

## Files Created

### Root

| File | Purpose |
|------|---------|
| `requirements.txt` | All Python dependencies pinned to compatible versions |
| `.env.example` | Template for all required environment variables |
| `Dockerfile` | Python 3.12-slim image, installs deps, runs uvicorn |
| `docker-compose.yml` | Orchestrates api, postgres:15, redis:7 with health checks |
| `BUILD_REPORT.md` | This file |

### app/

| File | Purpose |
|------|---------|
| `app/__init__.py` | Package marker |
| `app/main.py` | FastAPI app factory with lifespan (DB init, Redis ping), global exception handler, /health endpoint |
| `app/config.py` | Pydantic BaseSettings reading all 15 config keys from .env |
| `app/dependencies.py` | Shared FastAPI Depends: get_redis, get_current_user, require_admin, get_fraud_engine |

### app/core/

| File | Purpose |
|------|---------|
| `app/core/__init__.py` | Package marker |
| `app/core/security.py` | JWT creation/decoding (HS256), bcrypt password hashing via passlib |
| `app/core/exceptions.py` | 17 typed HTTP exception classes all returning structured error envelopes |

### app/db/

| File | Purpose |
|------|---------|
| `app/db/__init__.py` | Package marker |
| `app/db/base.py` | SQLAlchemy DeclarativeBase shared by all models |
| `app/db/session.py` | Async engine + AsyncSessionLocal factory + get_db generator |
| `app/db/init_db.py` | Creates all tables on startup via Base.metadata.create_all |

### app/models/

| File | Purpose |
|------|---------|
| `app/models/__init__.py` | Imports all 4 models to register them with metadata |
| `app/models/user.py` | `users` table: id, email, hashed_password, role, merchant_id, is_active, timestamps |
| `app/models/transaction.py` | `transactions` table: all 15 payment fields + merchant_id FK |
| `app/models/fraud_decision.py` | `fraud_decisions` table: all 3-layer outputs + final_decision, processing_ms |
| `app/models/rule.py` | `rules` table: name, rule_type, condition JSONB, action, is_active, created_by FK |

### app/schemas/

| File | Purpose |
|------|---------|
| `app/schemas/__init__.py` | Package marker |
| `app/schemas/auth.py` | RegisterRequest, LoginRequest, TokenResponse, AccessTokenResponse, UserResponse, etc. |
| `app/schemas/transaction.py` | TransactionCreate (with 10 validators), TransactionResponse |
| `app/schemas/fraud.py` | FraudDecisionResponse, FraudDecisionDetailResponse, LayersResponse + sub-schemas |
| `app/schemas/rule.py` | RuleCreate, RuleUpdate, RuleToggle, RuleResponse, PaginatedRules |

### app/services/

| File | Purpose |
|------|---------|
| `app/services/__init__.py` | Package marker |
| `app/services/rule_engine.py` | Layer 1: blacklist checks (Redis SISMEMBER), amount/velocity/geo rule evaluation, 60s rule cache, blacklist management methods |
| `app/services/behavioral_profiler.py` | Layer 2: Redis HASH profile read/write, Welford's online algorithm for rolling avg+stddev, 6 anomaly checks, velocity counter increments |
| `app/services/ai_scorer.py` | Layer 3: Anthropic claude-sonnet-4-6 API call, JSON response parsing, fallback on API errors |
| `app/services/fraud_engine.py` | Orchestrator: calls all 3 layers, short-circuits on BLOCK, applies decision matrix |
| `app/services/transaction_service.py` | Async CRUD: create transaction, save decision, get by ID, get decision by TX ID, paginated list |

### app/utils/

| File | Purpose |
|------|---------|
| `app/utils/__init__.py` | Package marker |
| `app/utils/geo.py` | haversine_km(), is_impossible_travel() using Haversine formula |

### app/api/v1/

| File | Purpose |
|------|---------|
| `app/api/v1/__init__.py` | Package marker |
| `app/api/v1/router.py` | Mounts auth, transactions, rules, admin sub-routers under /api/v1 |
| `app/api/v1/endpoints/__init__.py` | Package marker |
| `app/api/v1/endpoints/auth.py` | POST /register, /login, /refresh, /logout |
| `app/api/v1/endpoints/transactions.py` | POST /analyze, GET /{id}, GET /, GET /{id}/decision |
| `app/api/v1/endpoints/rules.py` | GET /, POST /, PUT /{id}, DELETE /{id}, PUT /{id}/toggle |
| `app/api/v1/endpoints/admin.py` | GET /metrics, GET /users, PUT /users/{id}/role |

### alembic/

| File | Purpose |
|------|---------|
| `alembic/alembic.ini` | Alembic configuration; sqlalchemy.url overridden at runtime from settings |
| `alembic/env.py` | Async migration runner using asyncpg; imports all models for autogenerate |

---

## Architecture Compliance

All files follow ARCHITECTURE.md exactly. No deviations.

| Requirement | Status |
|---|---|
| FastAPI async app | ✓ |
| SQLAlchemy 2.0 async ORM | ✓ |
| 4 PostgreSQL tables with correct schemas | ✓ |
| Redis behavioral profiles + velocity counters + blacklists | ✓ |
| Layer 1: rule engine with blacklist/amount/velocity/geo | ✓ |
| Layer 2: Welford rolling avg+stddev, 6 anomaly checks | ✓ |
| Layer 3: claude-sonnet-4-6, temp=0, 512 tokens, JSON parse | ✓ |
| Final decision matrix applied | ✓ |
| JWT HS256 access (30min) + refresh (7d) tokens | ✓ |
| Role-based access: merchant / admin | ✓ |
| Merchant ownership enforcement on GET /transactions | ✓ |
| Structured error envelopes for all exceptions | ✓ |
| /health endpoint with DB + Redis checks | ✓ |
| docker-compose with postgres:15 + redis:7 | ✓ |
| All functions have docstrings | ✓ |
| All I/O uses async/await | ✓ |

---

## For the Tester Agent

Start here:
1. All application code is in `app/`
2. Entry point: `app/main.py` — `app` is the FastAPI instance
3. External dependencies to mock: PostgreSQL (via SQLAlchemy AsyncSession), Redis (aioredis), Anthropic API (anthropic.AsyncAnthropic)
4. Key service boundaries:
   - `RuleEngine.evaluate()` — returns `RuleResult`
   - `BehavioralProfiler.score()` — returns `BehavioralResult`
   - `AIScorer.score()` — returns `AIResult`
   - `FraudEngine.analyze()` — returns `FraudDecisionResult`
5. Run with: `pytest tests/ -v --tb=short`
